---
title: 双指针
tags: [algorithm, two-pointer, technique]
aliases: [双指针, Two Pointer, 对撞指针, 快慢指针]
created: 2026-04-04
updated: 2026-04-04
---

# 双指针

双指针是一种通过维护两个指针在线性结构上移动来高效解决问题的技巧，能将 O(n²) 暴力优化到 O(n)。

## 三种模式

### 1. 快慢指针（同向）

用于链表操作：

```python
class ListNode:
    def __init__(self, val=0, next=None):
        self.val = val
        self.next = next

def has_cycle(head):
    """判断链表是否有环（LeetCode 141）"""
    slow = fast = head
    while fast and fast.next:
        slow = slow.next
        fast = fast.next.next
        if slow == fast:
            return True
    return False

def find_middle(head):
    """找链表中点"""
    slow = fast = head
    while fast and fast.next:
        slow = slow.next
        fast = fast.next.next
    return slow
```

### 2. 对撞指针（相向）

用于有序数组：

```python
def two_sum_sorted(nums, target):
    """有序数组两数之和（LeetCode 167）"""
    left, right = 0, len(nums) - 1
    while left < right:
        s = nums[left] + nums[right]
        if s == target:
            return [left, right]
        elif s < target:
            left += 1
        else:
            right -= 1
    return [-1, -1]

def is_palindrome(s):
    """判断回文"""
    left, right = 0, len(s) - 1
    while left < right:
        if s[left] != s[right]:
            return False
        left += 1
        right -= 1
    return True
```

### 3. 分离双指针

两个指针在不同数组上移动：

```python
def merge_sorted(arr1, arr2):
    """合并两个有序数组"""
    i = j = 0
    result = []
    while i < len(arr1) and j < len(arr2):
        if arr1[i] <= arr2[j]:
            result.append(arr1[i]); i += 1
        else:
            result.append(arr2[j]); j += 1
    result.extend(arr1[i:])
    result.extend(arr2[j:])
    return result
```

> [!tip] 复杂度分析
> - **时间**：O(n) — 每个元素至多被访问一次（或常数次）
> - **空间**：O(1) — 只用两个指针变量

> [!info] 使用前提
> - **对撞指针**：数组需有序（或可排序）
> - **快慢指针**：链表 / 环形结构
> - 双指针常配合排序预处理使用

## 关联

- [[算法-二分查找|二分查找]] — 也是双指针的特化（中间收缩）
- [[算法-滑动窗口|滑动窗口]] — 双指针的变长窗口模式
- [[链表]] — 快慢指针的典型应用领域
